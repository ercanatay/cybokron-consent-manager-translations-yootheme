## 2026-02-10 - WordPress Admin Tabs Accessibility
**Learning:** Custom tab implementations in WordPress admin pages frequently miss ARIA roles (`tablist`, `tab`, `tabpanel`) and keyboard navigation support, relying solely on visual cues (CSS classes).
**Action:** Always inspect custom UI components in admin panels for missing semantic roles and add standard ARIA patterns.
